# MatrixLabs Wallet

一个现代化的以太坊及EVM兼容链钱包浏览器插件，具有安全、简洁、高性能的特点。

## 功能特性

### 核心功能
- ✅ **钱包管理**
  - 创建新钱包（12词助记词）
  - 导入已有钱包
  - 多账户支持
  - 账户切换

- ✅ **安全特性**
  - AES-GCM加密存储
  - PBKDF2密钥派生
  - 密码保护
  - 助记词安全备份

- ✅ **网络支持**
  - 以太坊主网
  - Sepolia测试网
  - Polygon
  - BSC
  - 自定义RPC网络

- ✅ **交易功能**
  - 发送/接收代币
  - 交易签名
  - Gas费估算
  - 交易历史

- ✅ **dApp集成**
  - Web3 Provider注入
  - MetaMask兼容API
  - 交易确认弹窗
  - 消息签名

### UI设计特点

采用现代化、低对比度的设计风格：
- 🎨 **清冷色调**：深色背景配合低饱和度的蓝绿色调
- 🌫️ **玻璃态效果**：透明度和背景模糊营造层次感
- ✨ **流畅动画**：平滑的过渡和悬停效果
- 🎯 **无边界融合**：组件之间自然融合，避免生硬边框

## 技术栈

- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite
- **样式方案**: TailwindCSS
- **状态管理**: Zustand
- **区块链库**: ethers.js v6
- **加密库**: Web Crypto API + bip39

## 项目结构

```
matrixlabs-wallet/
├── src/
│   ├── popup/              # 弹窗UI
│   │   ├── pages/          # 页面组件
│   │   │   ├── Welcome.tsx
│   │   │   ├── CreateWallet.tsx
│   │   │   ├── ImportWallet.tsx
│   │   │   ├── Unlock.tsx
│   │   │   └── Home.tsx
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── components/         # 可复用组件
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Card.tsx
│   │   └── Modal.tsx
│   ├── lib/               # 核心库
│   │   ├── crypto.ts      # 加密服务
│   │   ├── wallet.ts      # 钱包服务
│   │   ├── storage.ts     # 存储服务
│   │   └── provider.ts    # RPC提供者
│   ├── store/             # 状态管理
│   │   └── wallet.ts
│   ├── background/        # 后台脚本
│   │   └── index.ts
│   ├── content/           # 内容脚本
│   │   └── index.ts
│   └── inpage/            # 页面注入脚本
│       └── index.ts
├── public/
│   └── icons/             # 图标资源
├── manifest.json          # 插件配置
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

## 开发指南

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist/` 目录。

### 加载到浏览器

1. 打开Chrome浏览器
2. 访问 `chrome://extensions/`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择 `dist/` 目录

## 安全说明

### 密钥管理
- 助记词使用AES-GCM加密存储
- 密码通过PBKDF2进行100,000次迭代
- 私钥永不离开本地环境

### 最佳实践
- ⚠️ **永远不要**分享你的助记词或私钥
- ⚠️ **务必备份**助记词到安全的离线位置
- ⚠️ **使用强密码**保护你的钱包
- ⚠️ **验证网站**在进行交易前确认网站真实性

## API文档

### WalletService

```typescript
// 创建钱包
WalletService.createWallet(password: string): Promise<string>

// 导入钱包
WalletService.importWallet(mnemonic: string, password: string): Promise<void>

// 解锁钱包
WalletService.unlockWallet(password: string): Promise<boolean>

// 签名交易
WalletService.signTransaction(tx: TransactionRequest): Promise<string>

// 签名消息
WalletService.signMessage(message: string): Promise<string>
```

### ProviderService

```typescript
// 获取余额
ProviderService.getBalance(address: string, network: Network): Promise<string>

// 发送交易
ProviderService.sendTransaction(signedTx: string, network: Network): Promise<TransactionResponse>

// 获取Gas价格
ProviderService.getGasPrice(network: Network): Promise<bigint>
```

## 路线图

- [ ] 硬件钱包支持
- [ ] NFT展示
- [ ] Token交换功能
- [ ] 地址簿
- [ ] 多语言支持
- [ ] 移动端适配

## 贡献指南

欢迎提交Issue和Pull Request！

## 许可证

MIT License

## 联系方式

- 项目主页: [GitHub Repository]
- 问题反馈: [Issues]

---

**⚠️ 免责声明**: 本钱包仅供学习和研究使用。使用前请确保理解区块链和加密货币的风险。开发者不对任何资产损失负责。
